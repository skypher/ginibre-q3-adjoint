# Exact `GKS2*` certificates for supports 260, 268, and 269 in `O_13`

Date: 2026-07-25

## Result

Every diagnostic direct-Hall failure regime at supports `260`, `268`, and
`269` in the rank-seven odd-orbit ring `O_13` has nonnegative scalar corner
for arbitrary residual exponents.

All regimes in this block admit one global rational capacitated
weighted-AM-GM allocation. No Farey fan, translated tail, cone subdivision,
or finite exact leaf is required.

## Exact verification

Candidate rational allocations were generated in four parallel workers. The
strict C++ verifier then independently:

- reconstructs every support/parity/residual spectral sum;
- isolates the seven real trace nodes with rational Sturm sequences;
- evaluates `B_1,...,B_6` with rational interval arithmetic;
- verifies all coefficient capacities exactly;
- verifies all logarithmic geometric inequalities with 512-bit MPFR
  intervals and directed rounding;
- audits the complete generated key set for the three supports.

The numerical optimization output is not treated as proof evidence. Only the
rational tables accepted by the strict algebraic replay contribute to the
result.

The recorded replay reports

```text
SU2_O13_SUPPORT260_269_EXACT PASS.
```

## Frontier consequence

Supports `260`, `268`, and `269` are removed from the unresolved rank-seven
frontier. The next exact target is the first diagnostic support after `269`.
Full `O_13`, and hence full `SU(2)_13`, remains open.

## Reproducibility status

The strict verifier and its local deterministic package were built and
replayed in the research runtime. The exact source archive has not yet been
persisted because the current connector repeatedly altered larger text
payloads. The exact PASS transcript is recorded in

```text
certificates/su2_o13_support260_269_exact.log.
```

Restoring the one-command archive remains a publication task.
