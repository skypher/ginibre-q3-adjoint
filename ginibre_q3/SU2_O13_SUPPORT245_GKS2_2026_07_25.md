# Exact `GKS2*` theorem for support 245 in `O_13`

Date: 2026-07-25

## Result

Use the rank-seven odd-orbit basis

```text
B_0=V_0, B_1=V_2, ..., B_6=V_12.
```

Support code `245` has signs

```text
B_1 minus, B_6 plus,
```

and parity code `2` gives minimum exponents two and one. Its three residual
faces are all nonnegative for arbitrary exponents:

```text
(B_1^-)^(4+2p) B_6^+,                    p>=0;
(B_1^-)^2 (B_6^+)^(3+2q),                q>=0;
(B_1^-)^(4+2p) (B_6^+)^(3+2q),           p,q>=0.
```

Thus every support-245 direct-Hall failure regime satisfies scalar `GKS2*`.

## 1. Exact transfer arithmetic

The two rays and the joint plane were first checked by integral orbit-fusion
transfer matrices. The recorded values are:

```text
B_1 ray:
0, 2, 110, 4398, 158738, 5437680, ...
only p=0 is zero through p=2000;

B_6 ray:
0, 0, 0, 2, 132, 5814, 225244, ...
only q=0,1,2 are zero through q=2000;

joint plane:
121 x 121 exact checks;
only (p,q)=(0,0) is zero.
```

Both ray sequences have degree-20 minimal recurrences. These scans and
recurrences are regression evidence; the all-exponent proof is the exact
certificate below.

## 2. Plane decomposition

Write `e_p=(1,0)`, `e_q=(0,1)`, and

```text
d=(1,3).
```

The nonnegative exponent quadrant is partitioned into:

1. a low cone based at `(2,0)` generated by `e_p,d`;
2. a high cone based at `(0,6)` generated by `d,e_q`;
3. twelve diagonal rays `q=3p+s`, `-6<=s<=5`, starting at `p=2`;
4. the two left-column tails `p=0,q>=6` and `p=1,q>=8`;
5. fourteen finite left-column points.

Together with the standalone ray decompositions, this gives

```text
18 infinite AM-GM pieces,
20 exact finite leaves.
```

No diagonal-ray finite leaves are needed.

## 3. Exact spectral certificates

The strict verifier:

- isolates the seven real trace nodes by rational Sturm sequences;
- evaluates `B_1,...,B_6` by rational interval arithmetic;
- reconstructs every shifted spectral coefficient and squared base;
- verifies all 18 denominator-100 capacitated weighted AM-GM allocations;
- checks the cone, strip, column, and ray geometry literally;
- evaluates all 20 finite leaves by integral fusion arithmetic.

Large logarithmic product inequalities use 512-bit MPFR intervals with
directed rounding. Floating optimization is used only to propose rational
weight tables and is not accepted as proof evidence.

The replay reports

```text
SU2_O13_SUPPORT245_EXACT PASS
pieces=18
 denominator=100
 leaves=20
 zeros=5
 minimum_leaf=0
 slope=3
 strips=12.
```

## 4. Reproduction

The deterministic package is a single base64 blob, small enough that it needs
no chunking:

```text
certificates/su2_o13_support245_package.b64
```

Its decoded SHA-256 is

```text
da31783b2297c692d7dbc4b10e9f41bc7a03b07b664b43c137403ad06995c76f.
```

Replay with

```text
character_ring_iter/replay_su2_o13_support245_package.sh
```

The source-matched transcript is

```text
certificates/su2_o13_support245_exact.log.
```

## 5. Frontier consequence

The exact rank-seven frontier now contains 175 proved direct-Hall failure
keys. The next support in order is support `248`, which has six diagnostic
failure regimes. Full `O_13`, and therefore full `SU(2)_13`, remains open.
