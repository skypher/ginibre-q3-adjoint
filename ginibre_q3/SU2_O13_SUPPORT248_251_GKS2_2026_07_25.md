# Exact `GKS2*` certificates for supports 248, 249, and 251 in `O_13`

Date: 2026-07-25

## Result

All direct-Hall failure regimes at supports `248`, `249`, and `251` in the
rank-seven odd-orbit ring `O_13` have nonnegative scalar corners for arbitrary
residual exponents.

The exact increment is

```text
support 248       6 regimes
support 249       2 regimes
support 251       9 regimes
                  --
total            17 regimes.
```

Together with the previously proved 175 keys, the exact `O_13` frontier now
contains 192 closed direct-Hall failure keys.

## 1. Support 248

Support `248` has active signs

```text
B_1 minus, B_2 plus, B_6 plus.
```

Five of its six regimes have global rational weighted AM-GM allocations. The
remaining `B_6`-axis ray is

```text
(B_1^-)^2 B_2^+ (B_6^+)^(3+2q).
```

Integral fusion arithmetic gives

```text
0, 2, 58, 1030, 15000, 196094, 2396950, ...
```

The initial value is the only zero leaf. After one residual shift, a
denominator-100 AM-GM allocation proves the complete tail. Hence all six
support-248 regimes are proved.

## 2. Support 249

Support `249` has signs

```text
B_2 minus, B_6 plus.
```

One face has a global allocation. The `B_6`-axis face has exact values

```text
0, 2, 110, 4398, 158738, 5437680, 180762400, ...
```

and a denominator-100 tail from residual floor two. Thus both support-249
regimes are proved.

## 3. Support 251: rays and global faces

Support `251` has signs

```text
B_1 minus, B_2 minus, B_6 plus.
```

Three one-variable rays reduce to finite prefixes followed by denominator-100
tails:

```text
even-parity B_6 ray:
16, 128, 1110, 12924, 194844, ...; tail floor 3;

odd-parity B_1 ray:
0, 10, 724, 32346, ...; tail floor 2;

odd-parity B_6 ray:
0, 0, 2, 612, 93650, ...; tail floor 3.
```

The two `B_2-B_6` residual faces admit global denominator-100 allocations.
These account for five of the nine support-251 regimes.

## 4. Support 251: mixed faces

The four remaining mixed faces require finite cone decompositions.

Three are covered by Farey fans:

```text
even B_1-B_6 face:      slope 1, four diagonal strips;
odd B_1-B_6 face:       slope 1, four diagonal strips;
odd B_1-B_2-B_6 face:   slope 1, two strips, B_2 independent.
```

These fans contain 31 infinite pieces. Every piece has an exact
weighted-AM-GM allocation with denominator 100.

The even three-variable face is covered recursively:

1. shift the `B_6` residual to floor three and certify the full
   three-variable tail;
2. certify the floor-two `B_1-B_2` face;
3. at floor one, shift `B_2` once and certify that tail;
4. certify the exposed `B_1` axis.

All four recursive allocations also have denominator 100. The strict replay
therefore checks

```text
31 Farey-fan pieces,
4 recursive translated-tail pieces,
35 pieces total.
```

## 5. Exact arithmetic

Both verifiers:

- isolate the seven trace nodes by rational Sturm sequences;
- evaluate all orbit characters with rational interval arithmetic;
- verify coefficient capacities exactly;
- verify logarithmic geometric inequalities with 512-bit MPFR intervals and
  directed rounding;
- evaluate every finite prefix with integral orbit-fusion arithmetic;
- reconstruct the fan and recursion geometry rather than trusting the
  allocation metadata.

The recorded PASS lines are

```text
SU2_O13_SUPPORT248_251_EXACT PASS
cases=13 finite_prefix_checks=7

SU2_O13_SUPPORT251_MIXED_EXACT PASS
pieces=35 fan_pieces=31 recursive_pieces=4 denominator=100.
```

## 6. Reproduction

The deterministic package is split into small audited base64 chunks:

```text
certificates/su2_o13_support248_251_package.b64.part00
certificates/su2_o13_support248_251_package.b64.part01
certificates/su2_o13_support248_251_package.b64.part02
```

The reconstructed archive has SHA-256

```text
0422d1fd71b20e3229730abfcd07f4b67d17497a1566e6f9c5b053733f37fdaf.
```

Replay with

```text
character_ring_iter/replay_su2_o13_support248_251_package.sh
```

The source-matched transcript is

```text
certificates/su2_o13_support248_251_exact.log.
```

## 7. Frontier consequence

The cumulative exact frontier now contains 192 proved direct-Hall failure
keys. The next unresolved support in exact order is support `259`, with 19
diagnostic failure regimes. Full `O_13`, and hence full `SU(2)_13`, remains
open.
