# Exact `GKS2*` theorem for support 259 in `O_13`

Date: 2026-07-25

## Result

Every direct-Hall failure regime at support code `259` in the rank-seven
odd-orbit ring `O_13` has nonnegative scalar corner for arbitrary residual
exponents.

The diagnostic frontier contains exactly 19 such residual regimes:

```text
parity 1:  residual 11,15
parity 2:  residual 3,11,15
parity 4:  residual 2,3,10,11,14,15
parity 7:  residual 8,9,10,11,12,13,14,15.
```

All 19 regimes admit one global capacitated weighted-AM-GM allocation. No
lattice subdivision, translated tail, or finite exact leaf is needed.

## Exact certificates

Eighteen regimes have denominator-100 rational allocation tables. One regime,
`parity=1, residual=15`, uses denominator 200. The numerically ill-conditioned
regime `parity=7, residual=9` was generated after logarithmic coefficient
rescaling, but its final table again has denominator 100.

The rescaling is a common positive normalization of all spectral masses and
therefore does not change any capacity inequality. Numerical optimization is
used only to propose integral weight tables.

The strict C++ verifier independently:

- isolates all seven real trace nodes with rational Sturm sequences;
- evaluates `B_1,...,B_6` using rational interval arithmetic;
- reconstructs each support/parity/residual spectral sum;
- checks every rational capacity inequality exactly;
- checks every logarithmic geometric inequality with 512-bit MPFR intervals
  and directed rounding;
- rejects missing, duplicate, or unexpected negative spectral terms.

Its recorded result is

```text
SU2_O13_SUPPORT259_EXACT PASS cases=19.
```

## Frontier consequence

The previous exact frontier contained 192 proved direct-Hall failure keys.
Support `259` contributes another 19, giving

```text
192 + 19 = 211
```

proved keys in the exact `O_13` frontier.

The next support in exact support order is support `260`.

## Reproducibility status

The strict source and its deterministic local replay package were built and
replayed in the research runtime. The current GitHub connector repeatedly
altered larger text payloads during this turn, so the source archive has not
yet been persisted to the repository. The theorem statement and exact PASS
transcript are recorded here and in

```text
certificates/su2_o13_support259_exact.log.
```

Restoring the one-command replay archive is a publication task; it does not
change the exact 19-case verification completed above.
